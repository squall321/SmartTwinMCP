{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "train_pytorch_gpu args",
  "type": "object",
  "additionalProperties": false,
  "required": ["train_script", "work_dir"],
  "properties": {
    "train_script": {
      "type": "string",
      "pattern": "^/.+\\.py$",
      "description": "Absolute path to the PyTorch training script (.py). Must exist on disk."
    },
    "work_dir": {
      "type": "string",
      "pattern": "^/.+",
      "description": "Absolute path to the working directory. sbatch stdout/stderr land here as train.slurm.{out,err}. Must exist and be writable."
    },
    "apptainer_sif": {
      "type": "string",
      "pattern": "^/.+\\.sif$",
      "default": "/opt/apptainers/pytorch_cuda12.sif",
      "description": "Absolute path to the apptainer (singularity) image containing PyTorch + CUDA on the compute nodes."
    },
    "extra_args": {
      "type": "array",
      "items": {"type": "string"},
      "default": [],
      "description": "Extra positional/flag arguments forwarded verbatim to the training script (e.g. ['--epochs', '50'])."
    },
    "time_limit": {
      "type": "string",
      "pattern": "^[0-9]{1,3}:[0-5][0-9]:[0-5][0-9]$",
      "default": "04:00:00",
      "description": "Slurm --time value (HH:MM:SS)."
    },
    "dry_run": {
      "type": "boolean",
      "default": true,
      "description": "If true (default), render and write the sbatch script but do NOT submit. Set false to actually submit."
    },
    "gpus": {
      "type": "integer",
      "minimum": 0,
      "maximum": 8,
      "default": 0,
      "description": "Number of GPUs to request. 0 = CPU-only (default). Must be >= 1 on a GPU partition."
    },
    "gpu_type": {
      "type": "string",
      "enum": ["a100", "h100", "rtx4090", "any"],
      "default": "any",
      "description": "GPU model selector. Maps to Slurm --gres=gpu:<type>:N. Use 'any' to skip the type filter."
    },
    "gpu_partition": {
      "type": "string",
      "enum": ["gpu-a100", "gpu-h100", "gpu-mixed"],
      "description": "Slurm partition. Required when gpus >= 1. Omit on CPU-only runs."
    }
  }
}
